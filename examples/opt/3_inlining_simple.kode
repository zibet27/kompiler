// Demonstrates function inlining: small functions expanded at call site
// Before: call instruction to add function
// After: add operation directly in main (no call overhead)

alien fun print_int{ x: i32 }: void;

fun add{ a: i32, b: i32 }: i32 (
    a + b;
);

fun main{}: i32 (
    i32 result = add{ 3, 4 };
    print_int{ result };
    0;
);

// Run with: ./gradlew run --args="examples/opt/3_inlining_simple.kode --print-opt"
// EXPECTED OUTPUT: 7
