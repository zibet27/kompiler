headers = llvm-c/Core.h
headerFilter = llvm-c/**

# Homebrew LLVM include directory
compilerOpts = -I/opt/homebrew/opt/llvm/include

# Link against common LLVM components
linkerOpts = -L/opt/homebrew/opt/llvm/lib \
             -lLLVMCore -lLLVMSupport -lLLVMIRReader